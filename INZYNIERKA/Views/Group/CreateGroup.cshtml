@{
    ViewData["Title"] = "Create Group";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.css" />

<div class="container py-5 d-flex justify-content-center">
    <div class="card-group-form p-5 w-100" style="max-width: 500px;">
        
        <div class="text-center mb-4">
            <i class="bi bi-collection-fill" style="font-size: 3rem; color: #552459;"></i>
            <h2 class="fw-bold mt-3">Create New Group</h2>
            <p class="text-muted">Start a new community and invite others</p>
        </div>

        <form asp-action="CreateGroup" method="post">
            <div class="mb-4">
                <label for="groupName" class="form-label fw-bold text-muted">Group Name:</label>
                <input type="text" class="form-control my-input" id="groupName" name="name" placeholder="e.g. Star Wars fans..." required />
            </div>

            <button type="submit" class="btn-group-edit gradient-main w-100">
                <i class="bi bi-plus-circle me-2"></i> Create Group
            </button>
        </form>

    </div>
</div>